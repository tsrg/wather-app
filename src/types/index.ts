export interface getCityWeatherData {
  lat: number,
  lon: number,
}

export interface coords {
  lat: number,
  lon: number
}

interface weather {
  id: number,
  main: string,
  description: string,
  icon: string
}

export interface city {

}
